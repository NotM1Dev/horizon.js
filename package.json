{
  "name": "@horizon-js/workspace",
  "private": true,
  "author": "M1",
  "scripts": {
    "build": "turbo build",
    "test": "turbo test",
    "lint": "turbo lint; prettier --check . --ignore-path=.prettierignore",
    "format": "prettier --write .",
    "deploy:horizon.js": "cd ./packages/horizon.js && pnpm publish --no-git-checks"
  },
  "devDependencies": {
    "@horizon-js/internals": "workspace:*",
    "prettier": "^3.1.0",
    "turbo": "latest"
  },
  "packageManager": "pnpm@8.9.0",
  "engines": {
    "node": ">=18"
  }
}
